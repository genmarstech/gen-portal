/**
 * Auth group layout.
 *
 * Nothing here yet beyond grouping — AuthShell is applied per page so each
 * screen sets its own eyebrow, title and panel copy.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return children;
}
