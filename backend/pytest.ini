[pytest]
DJANGO_SETTINGS_MODULE = config.settings_test
python_files = test_*.py
addopts = -q --strict-markers

# --strict-markers above means an unregistered marker is an error rather than
# a silently ignored typo, so every marker is declared here.
markers =
    seeded: keep the rows a data migration wrote, instead of starting empty.
